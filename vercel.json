{
  "version": 2,
  "builds": [
    { "src": "build.sh", "use": "@vercel/static-build", "config": { "distDir": "app" } }
  ],
  "routes": [
    { "src": "/.*", "dest": "/app.py", "methods": ["GET", "POST"] }
  ]
}
